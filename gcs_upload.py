from google.cloud import storage\nimport os\n\ndef upload_blob(bucket_name, source_file_name, destination_blob_name):\n    \"\"\"Uploads a file to the bucket.\n    Args:\n        bucket_name: Name of the GCS bucket.\n        source_file_name: Path to the file to upload.\n        destination_blob_name: Name of the object in the bucket.\n    \"\"\"\n    storage_client = storage.Client()\n    bucket = storage_client.bucket(bucket_name)\n    blob = bucket.blob(destination_blob_name)\n    blob.upload_from_filename(source_file_name)\n    print(f'File {source_file_name} uploaded to {destination_blob_name}.')\n\nif __name__ == '__main__':\n    # Example usage\n    # These values should be replaced with your actual bucket and file names\n    bucket = os.environ.get('GCS_BUCKET') or 'your-bucket-name'\n    local_file = 'example.txt'\n    remote_file = 'uploads/example.txt'\n    upload_blob(bucket, local_file, remote_file)\n
